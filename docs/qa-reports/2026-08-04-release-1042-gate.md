# QA Batch Gate — release 1.0.42

Date: 2026-08-04
Scope: 6 merged changes fixing 8 issues (#139, #140, #141, #142, #143, #144, #145 + related) prior to release 1.0.42.

## Verdict: **FAIL** — blocking regression found

> **Lead resolution (2026-08-04, after this report was written):** the version-sync failure below was a **race with the release commit in progress**, not a regression. QA's suite ran for 424s while Lead was mid-bump: `pyproject.toml` + `package.json` were already at 1.0.42 and `src/agent_takkub/__init__.py` was not yet. All three landed together in `2e206ea`, and `tests/test_version_sync.py` passes on that commit — CI for `2e206ea` is **green on ubuntu + windows + macOS (lint-and-test and installed-gate, 5/5 jobs)**. Shipping was not blocked.
> The `doctor --live` item stays open as noted: verifying it needs a cockpit restart, which only Lead can do.

## 1. Full test suite

`.venv/Scripts/python.exe -m pytest -q --junitxml=qa_junit.xml`

```
tests=4917  errors=0  failures=1  skipped=5  time=423.99s
```

**FAILED**: `tests/test_version_sync.py::test_python_and_npm_versions_match_pyproject`

```
AssertionError: assert '1.0.41' == '1.0.42'
  - 1.0.42   (pyproject.toml / package.json)
  + 1.0.41   (agent_takkub.__version__)
```

`pyproject.toml` and `package.json` were bumped to `1.0.42` but `src/agent_takkub/__version__.py` (or equivalent) was not updated to match. **Blocks release** — this is exactly the drift `test_version_sync.py` exists to catch.

## 2. Lint / static checks

| Check | Result |
|---|---|
| `ruff check src tests` | ✅ All checks passed |
| `ruff format --check src tests` | ✅ 370 files already formatted |
| `lint-imports` (import-linter, 23 contracts) | ✅ 23 kept, 0 broken |

## 3. Smoke tests

| # | Check | Result |
|---|---|---|
| 1 | `takkub doctor` (no cockpit) | ✅ pass — `ok: all checks passed`, 27 ok / 1 skip / 4 info, no `[spawn-queue]` section (correct — that section only appears with `--live`) |
| 2 | `takkub doctor --live` (cockpit running) | ⚠️ **Inconclusive, not a code bug** — see below |
| 3 | `takkub issue list` | ✅ pass — shows real open issues (#127, #139–146) with `scope:` line naming the cockpit repo store; no longer `(no issues)` |
| 4 | `takkub issue list --no-cockpit-bug` | ✅ pass — reads the project store without crashing (agent-takkub is its own project here, so output matches default) |
| 5 | `takkub status` | ✅ pass — raw bytes inspected, zero `\x1b` (ESC) bytes, no `?25h`/`?25l`/`[3G`/`]0;` sequences left in output |
| 6 | `takkub assign --role backend --cwd C:/Windows/Temp "noop"` | ⚠️ Could not exercise directly as `qa` — see below |

### #2 detail — `takkub doctor --live`
Got `⚠ wedge  live check failed: unknown cmd: spawn-queue-status` instead of `ok`/detail. Root-caused: the running dev cockpit instance (port 61501, confirmed via `takkub list` and `runtime/port`) is the correct target — port routing is fine — but that **already-running process** was started before commit `b4e6905` (`fix(#141): takkub doctor --live sees spawn-queue wedges`) landed, so its in-memory `cli_server.py` doesn't have the `spawn-queue-status` handler yet. This is process staleness, not a code defect — `git log` confirms `b4e6905` and the cwd-validation fix (`0b354ca`) are both already on `main`. Only `lead` can run `takkub restart` (confirmed: `qa` gets `error: only lead can run 'takkub restart'`), so I could not force a reload myself. **Recommend Lead run `takkub restart` then re-run `takkub doctor --live` to get a clean read before signing off on #141.**

### #6 detail — `takkub assign --cwd` validation
`qa` is role-gated from calling `takkub assign` at all (`error: only lead can run 'takkub assign'. you are 'qa'.`), so the CLI-level repro asked for in the task spec isn't reachable from this pane — expected/correct gate behavior, not a bug. Verified the underlying logic instead: `tests/test_cli_server.py::TestSyncCwdValidation` (5 tests, covering `#143`'s synchronous-reject-before-ack behavior including `test_assign_accepts_project_root_cwd`) — all 5 pass, and were part of the green full-suite run above. Lead can re-run the literal CLI repro directly since only Lead can invoke `assign`.

### #5 skipped
`takkub assign --role qa --model claude-haiku-4-5 "noop"` — skipped per task instructions (qa=claude passes validation; would spawn a real pane).

## Summary

- **Blocking:** version sync failure (1.0.41 vs 1.0.42) — must fix before shipping 1.0.42.
- **Non-blocking, needs Lead action:** `doctor --live` needs the dev cockpit restarted to verify #141 cleanly (process predates the fix).
- Everything else (lint, import-linter, issue list ×2, status ANSI-clean, #143 cwd validation via unit tests) is green.
