[project]
name = "agent-takkub"
version = "1.0.42"
description = "Desktop cockpit for orchestrating Claude Code dev teammates on Windows"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    # Pinned to the Qt 6.8 LTS series. Qt 6.11.0 shipped a Qt6Core.dll regression
    # that aborts the cockpit with 0xc0000409 (__fastfail) — a hard native crash
    # with no Python traceback, observed recurring in the field (boot.log shows
    # no watchdog stall; Windows WER faults in QtCore). 6.8 is the battle-tested
    # LTS. Pin the Qt6 *binary* packages too (they carry Qt6Core.dll itself), not
    # just the bindings, so a resolver can't pull 6.11 back in. Bump deliberately.
    "PyQt6>=6.8,<6.9",
    "PyQt6-Qt6>=6.8,<6.9",
    "PyQt6-WebEngine>=6.8,<6.9",
    "PyQt6-WebEngine-Qt6>=6.8,<6.9",
    "pywinpty>=2.0;sys_platform=='win32'",
    "ptyprocess>=0.7;sys_platform!='win32'",
    "pyte>=0.8",
    "pyyaml>=6",
    "markdown>=3",
    "psutil>=5.9",
]

[project.scripts]
agent-takkub = "agent_takkub.app:main"
agent-takkub-headless = "agent_takkub.headless:main"
takkub = "agent_takkub.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=8",
    # Pinned, not floating: CI installs .[dev] fresh, so an unpinned ruff let
    # a newer release land there than the one developers run locally — the
    # local gate passed while CI failed on a rule the old version didn't have.
    # Bump this and .pre-commit-config.yaml's rev together, deliberately.
    "ruff==0.16.1",
    "pre-commit>=3.7",
    "grimp>=3.14",        # dep-graph generation for tools/gen_import_graph.py
    "import-linter>=2.9", # architecture contracts (lint-imports)
    "build>=1.0",         # wheel builder used by the installed-mode gate tests
]

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
agent_takkub = [
    "static/*.html",
    "static/*.js",
    "static/*.css",
    "static/*.png",
    "static/fonts/*.ttf",
    "static/fonts/LICENSE.txt",
    "static/fonts/LICENSE-NotoSansThai.txt",
    "static/icons/*.svg",
    # Staged by setup.py's custom build_py from the repo-root CLAUDE.md /
    # .claude/agents/*.md / .claude/skills/*/SKILL.md right before this list
    # is collected — see config.py's ASSETS_ROOT for the
    # installed-vs-dev-checkout read path.
    "_assets/CLAUDE.md",
    "_assets/.claude/agents/*.md",
    "_assets/.claude/skills/*/SKILL.md",
    # remote/ (P0 scaffold) mobile PWA — glob stays empty (no error) until
    # P1 adds files; deleting remote/ makes it empty again, build still works.
    "remote/static/*",
]

# ──────────────────────────────────────────────────────────────────
# ruff: lint + format
# ──────────────────────────────────────────────────────────────────
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]
extend-exclude = [
    "scripts/test_*.py",
    "scripts/inspect_*.py",
    "runtime/**",
    # Throwaway per-pane checkouts (`--isolation worktree`): a full copy of the
    # tree that ruff would lint twice, and that CI never sees at all.
    "worktrees/**",
    # ruff 0.16 formats python fenced in markdown. Prose docs are not part of
    # the build; letting the formatter rewrite them churns docs on every ruff
    # bump for no gain.
    "*.md",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "RUF"]
ignore = [
    "E501",   # line too long; let `format` handle wrapping
    "B008",   # function call in default arg (PyQt signal defaults are fine)
    "RUF001", # ambiguous unicode chars (we use Thai + diacritics on purpose)
    "RUF002",
    "RUF003",
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]  # asserts in tests are intentional
"src/agent_takkub/orchestrator.py" = ["F401"]  # re-export façade — moved symbols re-exported for tests/app/main_window

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

# ──────────────────────────────────────────────────────────────────
# pytest
# ──────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"

# ──────────────────────────────────────────────────────────────────
# import-linter: architecture guardrails
# ──────────────────────────────────────────────────────────────────
[tool.importlinter]
root_packages = ["agent_takkub"]

[[tool.importlinter.contracts]]
name = "engine-ui-separation"
type = "forbidden"
source_modules = ["agent_takkub.orchestrator"]
forbidden_modules = [
    "agent_takkub.main_window",
    "agent_takkub.app",
]

[[tool.importlinter.contracts]]
name = "cli-ipc-boundary"
type = "forbidden"
# Intent: cli.py drives the running cockpit over the TCP socket (cli_server), it must NOT
# pull the 5.8k-LOC orchestrator engine into the CLI process. _SAFE_PLUGINS was moved to
# config.py (leaf hub) so doctor no longer imports orchestrator, removing the indirect edge.
source_modules = ["agent_takkub.cli"]
forbidden_modules = ["agent_takkub.orchestrator"]

[[tool.importlinter.contracts]]
name = "leaf-modules-pure"
type = "forbidden"
# config/roles/_win_console are pure-leaf (fan-in 18/top-used). List all architectural
# siblings so any future sibling import is caught immediately.
source_modules = [
    "agent_takkub.config",
    "agent_takkub._win_console",
    "agent_takkub.roles",
    "agent_takkub.system_baseline",
]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.agent_pane",
    "agent_takkub.agent_pane_model",
    "agent_takkub.cli_server",
    "agent_takkub.cli",
    "agent_takkub.pty_session",
    "agent_takkub.lead_context",
    "agent_takkub.pane_env",
    "agent_takkub.vault_mirror",
    "agent_takkub.routing_planner",
    "agent_takkub.project_tab",
    "agent_takkub.terminal_widget",
    "agent_takkub.spawn_gate",
    "agent_takkub.provider_config",
    "agent_takkub.claude_auth_config",
    "agent_takkub.claude_auth_dialog",
    "agent_takkub.gemini_helper",
    "agent_takkub.codex_helper",
    "agent_takkub.pipeline_config",
    "agent_takkub.lead_bash_audit",
    "agent_takkub.vault_graph",
    "agent_takkub.docs_verify",
    "agent_takkub.doctor",
    "agent_takkub.services",
    "agent_takkub.issues",
    "agent_takkub.token_meter",
    "agent_takkub.limit_status",
    "agent_takkub.chatlog_scanner",
    "agent_takkub.logs_panel",
    "agent_takkub.project_rules",
    "agent_takkub.role_memory",
    "agent_takkub.shared_dev_tools",
    "agent_takkub.user_profile",
    "agent_takkub.skill_audit",
    "agent_takkub.skill_scan",
    "agent_takkub.verify",
    "agent_takkub.update_helper",
    "agent_takkub.update_worker",
    "agent_takkub.rtk_helper",
    "agent_takkub.plan_tier",
    "agent_takkub.design_review_html",
    "agent_takkub.release",
    "agent_takkub.claude_update",
    "agent_takkub.codex_agents_md",
    "agent_takkub.provider_state",
    "agent_takkub.claude_auth_config",
]

[[tool.importlinter.contracts]]
name = "pipeline-executor-layer"
type = "forbidden"
# pipeline_executor is an engine-layer module (mixed into Orchestrator). It must
# NOT pull in UI or CLI modules — same constraint as orchestrator itself.
source_modules = ["agent_takkub.pipeline_executor"]
forbidden_modules = [
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "orchestrator-text-layer"
type = "forbidden"
# orchestrator_text is a pure-leaf helper (no Qt, no self). It must not import
# UI/CLI modules or orchestrator itself (would create a circular import).
source_modules = ["agent_takkub.orchestrator_text"]
forbidden_modules = [
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
    "agent_takkub.orchestrator",
]

[[tool.importlinter.contracts]]
name = "lead-inbox-layer"
type = "forbidden"
# lead_inbox is an engine-layer mixin — must not import orchestrator (cycle) or UI/CLI.
source_modules = ["agent_takkub.lead_inbox"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "limit-autoresume-layer"
type = "forbidden"
# limit_autoresume is an engine-layer mixin (mixed into Orchestrator) — must
# not import orchestrator (cycle) or UI/CLI, same constraint as lead_inbox.
source_modules = ["agent_takkub.limit_autoresume"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "update-panel-layer"
type = "forbidden"
# update_panel is a UI-layer mixin (mixed into MainWindow). It must not pull in
# app or cli — same constraint as main_window itself.
source_modules = ["agent_takkub.update_panel"]
forbidden_modules = [
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "worktree-manager-leaf"
type = "forbidden"
# worktree_manager (#81) is a pure leaf: stdlib + config only. It's imported by
# the engine (orchestrator) AND the leaf helper orchestrator_text, so it must
# stay dependency-free of UI/CLI/engine or it would create an import cycle.
source_modules = ["agent_takkub.worktree_manager"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.orchestrator_text",
    "agent_takkub.spawn_engine",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
    "agent_takkub.cli_server",
    "agent_takkub.agent_pane",
]

[[tool.importlinter.contracts]]
name = "disk-usage-layer"
type = "forbidden"
# disk_usage (`takkub disk`/`prune`) is a pure leaf like worktree_manager: cli.py
# imports it directly (no orchestrator socket — same pure-local rationale as
# `worktree`), so it must stay free of orchestrator/UI or that edge becomes a
# transitive cli->orchestrator violation of cli-ipc-boundary.
source_modules = ["agent_takkub.disk_usage"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
    "agent_takkub.cli_server",
]

[[tool.importlinter.contracts]]
name = "project-wizard-layer"
type = "forbidden"
# project_wizard is a UI-layer mixin (mixed into MainWindow). It must not pull
# in app or cli — same constraint as main_window itself.
source_modules = ["agent_takkub.project_wizard"]
forbidden_modules = [
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "user-actions-layer"
type = "forbidden"
# user_actions is a UI-layer mixin (mixed into MainWindow). It must not pull
# in app or cli — same constraint as main_window itself.
source_modules = ["agent_takkub.user_actions"]
forbidden_modules = [
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "limit-panel-layer"
type = "forbidden"
# limit_panel is a UI-layer mixin (mixed into MainWindow). It must not pull
# in app or cli — same constraint as main_window itself.
source_modules = ["agent_takkub.limit_panel"]
forbidden_modules = [
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "provider-spec-pure"
type = "forbidden"
# provider_spec (issue #103 Phase 0) is a pure data layer (dataclass registry).
# provider_config/spawn_engine/pty_session/orchestrator_text all import it, so
# it must stay free of all of them (and the engine/UI/CLI layers) or the edge
# becomes a cycle.
source_modules = ["agent_takkub.provider_spec"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
    "agent_takkub.cli_server",
    "agent_takkub.spawn_engine",
    "agent_takkub.pty_session",
    "agent_takkub.orchestrator_text",
    "agent_takkub.lead_inbox",
    "agent_takkub.provider_config",
]

[[tool.importlinter.contracts]]
name = "spawn-engine-layer"
type = "forbidden"
# spawn_engine is a pure-logic mixin. It must never import from the UI layer
# or the orchestrator itself (which would create a cycle).
source_modules = ["agent_takkub.spawn_engine"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "status-header-layer"
type = "forbidden"
# status_header is a UI-layer mixin (mixed into MainWindow). It must not pull
# in app or cli — same constraint as main_window itself.
source_modules = ["agent_takkub.status_header"]
forbidden_modules = [
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "usage-meter-layer"
type = "forbidden"
# usage_meter is a pure-leaf UI widget — must not import orchestrator, app, or cli.
source_modules = ["agent_takkub.usage_meter"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "tutorial-overlay-layer"
type = "forbidden"
# tutorial_overlay is a pure-leaf UI widget — must not import orchestrator, app, or cli.
source_modules = ["agent_takkub.tutorial_overlay"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.app",
    "agent_takkub.cli",
]

[[tool.importlinter.contracts]]
name = "remote-bolt-on-isolation"
type = "forbidden"
# remote/ (P0 scaffold — remote-control-plan/2026-07-07-remote-control.md) is a
# delete-to-uninstall bolt-on: the rest of the codebase must never structurally
# depend on it. The one core touch point (main_window.py::_boot) reaches it via
# importlib.import_module, which grimp doesn't see as a static edge, so this
# contract stays green with the wiring in place *and* after `rm -rf remote/`.
# source can't be the umbrella `agent_takkub` (remote is its descendant →
# import-linter "shared descendants" error); list the entry points + engine +
# every UI/CLI module that could plausibly wire remote instead.
source_modules = [
    "agent_takkub.app",
    "agent_takkub.main_window",
    "agent_takkub.cli",
    "agent_takkub.cli_server",
    "agent_takkub.orchestrator",
    "agent_takkub.spawn_engine",
    "agent_takkub.agent_pane",
    "agent_takkub.pipeline_executor",
    "agent_takkub.lead_inbox",
    "agent_takkub.update_panel",
    "agent_takkub.project_wizard",
    "agent_takkub.user_actions",
    "agent_takkub.status_header",
    "agent_takkub.doctor",
    "agent_takkub.config",
]
forbidden_modules = ["agent_takkub.remote"]

[[tool.importlinter.contracts]]
name = "settings-management-layer"
type = "forbidden"
# settings_management/ (#103 follow-up — Settings redesign) is a bolt-on
# module tree that adapts existing data-layer modules; it must never pull
# in the engine/UI-chrome/CLI layers it coexists alongside (SPEC.md
# "Coexistence" — legacy settings_window.py and the new module tree share
# storage but never import each other or the orchestrator/app/cli entry
# points). source can't be the umbrella `agent_takkub` (settings_management
# is its descendant -> import-linter "shared descendants" error).
source_modules = ["agent_takkub.settings_management"]
forbidden_modules = [
    "agent_takkub.orchestrator",
    "agent_takkub.app",
    "agent_takkub.cli",
    "agent_takkub.main_window",
    "agent_takkub.settings_window",
]
# Sole sanctioned exception: the standalone dev entry point's "Open legacy
# settings" hook (critic R3 MED — the button was a dead no-op in standalone
# mode without it). Function-level lazy import, one-way convenience glue for
# `python -m agent_takkub.settings_management` only — the window/pages/
# repositories tree itself still never touches legacy.
ignore_imports = [
    "agent_takkub.settings_management.__main__ -> agent_takkub.settings_window",
]

[[tool.importlinter.contracts]]
name = "agent-pane-model-headless"
type = "forbidden"
# agent_pane_model (issue #105 Phase A) is the display-free half of the
# AgentPane split — session/state bookkeeping the orchestrator's watchdogs
# read, importable without QtWidgets/QtWebEngineWidgets/a display so a
# future headless engine (Phase B) can construct it directly. It must never
# import the view (agent_pane, terminal_widget) or UI/CLI/app entry points —
# a leak here would silently reintroduce the display dependency this split
# exists to remove.
source_modules = ["agent_takkub.agent_pane_model"]
forbidden_modules = [
    "agent_takkub.agent_pane",
    "agent_takkub.terminal_widget",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
    "agent_takkub.orchestrator",
]

[[tool.importlinter.contracts]]
name = "headless-pane-no-display"
type = "forbidden"
# headless_pane (#105 Phase B) is the QObject-signal adapter that lets
# HeadlessPane sit in the pane registry alongside AgentPane — it wraps
# agent_pane_model.AgentPaneModel but must stay just as display-free: no
# QWidget/QWebEngineView, no orchestrator (headless_window imports THIS
# module, never the other way — importing orchestrator here would cycle).
source_modules = ["agent_takkub.headless_pane"]
forbidden_modules = [
    "agent_takkub.agent_pane",
    "agent_takkub.terminal_widget",
    "agent_takkub.main_window",
    "agent_takkub.app",
    "agent_takkub.cli",
    "agent_takkub.orchestrator",
]

[[tool.importlinter.contracts]]
name = "headless-window-no-display"
type = "forbidden"
# headless_window (#105 Phase B) is MainWindow's display-free counterpart —
# every pane it creates is a HeadlessPane, never a real AgentPane. It must
# never import the desktop entry points directly (main_window, app). NOTE:
# it can't also forbid agent_pane/terminal_widget here — it legitimately
# imports orchestrator, which still imports agent_pane today for one
# isinstance() check (existing architecture, not a Phase B leak); import-
# linter's "forbidden" check is transitive, so that indirect edge would
# always trip. headless_window's real "no display" guarantee is that it
# never *constructs* an AgentPane/QWidget — see its module docstring — which
# is a runtime property, not an import-graph one.
source_modules = ["agent_takkub.headless_window"]
forbidden_modules = [
    "agent_takkub.main_window",
    "agent_takkub.app",
]
